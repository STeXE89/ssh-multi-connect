# ssh2 pulls in cpu-features/nan as optional native dependencies. They are only
# a cipher-selection optimisation, and a copy built against Node's ABI
# segfaults the Electron extension host on activation (require('ssh2') dies
# with SIGSEGV). ssh2 already guards the import, so omitting them is safe.
omit=optional
